/** Matches backend `resource.action` codes (see permissions catalog / role matrix). */
export const ADMIN_ROLE = "ADMIN";

/** In-app notification inbox + bell (GET /notifications, etc.). */
export const PERMISSION_NOTIFICATIONS_READ = "notifications.read";
