export interface SalesPartnerType {
    id: string;
    fullName: string;
    email: string;
    phone: string;
    address: string;
    company: string;
    description: string;
    createdAt: string;
    updatedAt: string;
}